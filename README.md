Дратути! Это первая бета версия моей недо ИИ!
кароче, вот все зависимости для python:

# GUI
pip install PyQt5

# Local AI
pip install transformers
pip install torch

# TTS (голос)
pip install gTTS
pip install pygame

# Для работы с аудио
pip install pydub
pip install simpleaudio

# Дополнительно (для Windows)
pip install pywin32
